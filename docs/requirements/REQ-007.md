# REQ-007: Connection Profiles

**ID:** REQ-007
**Domain:** Connection
**Status:** Draft
**Priority:** P1 (Important)

## Description

The application MUST support multiple server connection profiles.

## Acceptance Criteria

- [ ] Add/edit/delete server profiles (UI needed — stored in QSettings)
- [x] Store: name, host, port (ConfigManager + ServerProfile model)
- [ ] Quick switch between saved profiles
- [x] Profiles persist across app restarts (QSettings)

## Implementation Notes

- Backend storage already implemented in `src/snapctrl/core/config.py` (ConfigManager)
- ServerProfile model in `src/snapctrl/models/profile.py`
- Missing: UI for profile management (add/edit/delete dialog, quick-switch menu)
