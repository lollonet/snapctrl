name: Build & Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

env:
  VERSION: ${{ github.ref_name || 'dev' }}

jobs:
  build-macos:
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Verify spec file exists
        run: test -f SnapCTRL-macos.spec
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: pip install pyinstaller PySide6 zeroconf
      - name: Build with PyInstaller
        run: pyinstaller SnapCTRL-macos.spec
      - name: Smoke test
        run: |
          test -f dist/SnapCTRL.app/Contents/MacOS/SnapCTRL
          test -f dist/SnapCTRL.app/Contents/Info.plist
      - name: Add build metadata
        run: |
          echo "Commit: ${{ github.sha }}" > dist/BUILD_INFO.txt
          echo "Ref: ${{ github.ref_name }}" >> dist/BUILD_INFO.txt
          echo "Built: $(date -u +%Y-%m-%dT%H:%M:%SZ)" >> dist/BUILD_INFO.txt
      - name: Zip app bundle
        run: cd dist && zip -r "SnapCTRL-macos-${VERSION}.zip" SnapCTRL.app BUILD_INFO.txt
      - uses: actions/upload-artifact@v4
        with:
          name: SnapCTRL-macos
          path: dist/SnapCTRL-macos-*.zip
          retention-days: 7

  build-windows:
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Verify spec file exists
        run: Test-Path SnapCTRL-windows.spec -ErrorAction Stop
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: pip install pyinstaller PySide6 zeroconf
      - name: Build with PyInstaller
        run: pyinstaller SnapCTRL-windows.spec
      - name: Smoke test
        run: Test-Path dist\SnapCTRL\SnapCTRL.exe -ErrorAction Stop
      - name: Add build metadata
        run: |
          echo "Commit: ${{ github.sha }}" > dist\SnapCTRL\BUILD_INFO.txt
          echo "Ref: ${{ github.ref_name }}" >> dist\SnapCTRL\BUILD_INFO.txt
          echo "Built: $(Get-Date -Format o)" >> dist\SnapCTRL\BUILD_INFO.txt
      - name: Zip output
        run: Compress-Archive -Path dist\SnapCTRL -DestinationPath "dist\SnapCTRL-windows-${env:VERSION}.zip"
      - uses: actions/upload-artifact@v4
        with:
          name: SnapCTRL-windows
          path: dist/SnapCTRL-windows-*.zip
          retention-days: 7

  build-linux:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Verify spec file exists
        run: test -f SnapCTRL-linux.spec
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libegl1 libxcb-icccm4 libxcb-image0 \
            libxcb-keysyms1 libxcb-randr0 libxcb-render-util0 \
            libxcb-xinerama0 libxcb-xfixes0 libxkbcommon-x11-0
      - name: Install Python dependencies
        run: pip install pyinstaller PySide6 zeroconf
      - name: Build with PyInstaller
        run: pyinstaller SnapCTRL-linux.spec
      - name: Smoke test
        run: test -f dist/SnapCTRL/SnapCTRL && test -x dist/SnapCTRL/SnapCTRL
      - name: Add build metadata
        run: |
          echo "Commit: ${{ github.sha }}" > dist/SnapCTRL/BUILD_INFO.txt
          echo "Ref: ${{ github.ref_name }}" >> dist/SnapCTRL/BUILD_INFO.txt
          echo "Built: $(date -u +%Y-%m-%dT%H:%M:%SZ)" >> dist/SnapCTRL/BUILD_INFO.txt
      - name: Zip output
        run: cd dist && zip -r "SnapCTRL-linux-${VERSION}.zip" SnapCTRL
      - uses: actions/upload-artifact@v4
        with:
          name: SnapCTRL-linux
          path: dist/SnapCTRL-linux-*.zip
          retention-days: 7

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [build-macos, build-windows, build-linux]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true
      - name: Generate checksums
        run: cd artifacts && sha256sum *.zip > SHA256SUMS
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            artifacts/SnapCTRL-*.zip
            artifacts/SHA256SUMS
